var dishes = $("#dishes")
if (dishes.length)
	dishes.append("<%= escape_javascript(render(@dishes)) %>");
else{
	<% @k = 0 if params[:page].blank? %>
	$("#wrapper").html("<%= escape_javascript(render :file => "dishes/index.html.haml") %>");
}